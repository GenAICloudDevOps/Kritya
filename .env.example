# Copy to .env and fill in your own keys. .env is gitignored — never commit it.

# get your key at https://build.nvidia.com (any model page -> "Get API Key")
NVIDIA_API_KEY=

# powers the agent's web_search tool and /web-search
TAVILY_API_KEY=
