apiVersion: 1
providers:
  - name: kritya
    type: file
    options:
      # Relative to the working directory grafana-server is launched from —
      # per docs/observability.md, that's the repo root (there's no Docker
      # container here, so /etc/grafana/... would never exist).
      path: observability/grafana/provisioning/dashboards
